name: Build MybknCore Plugin

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn -B clean package --file MybknCore/pom.xml

      - name: Upload built jar
        uses: actions/upload-artifact@v4
        with:
          name: MybknCore-jar
          path: MybknCore/target/MybknCore.jar
